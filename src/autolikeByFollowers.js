const chalk = require('chalk');
console.log(chalk.cyan('\n=== BOT LIKE TIMELINE / FOLLOW + LIKE FOLLOWERS TARGET ===\n'));
console.log(chalk.yellow('Fitur dalam pengembangan. Akan segera hadir!'));
process.exit(0); 